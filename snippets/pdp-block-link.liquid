{% comment %}
  Renders product page image block

  Accepts:
  - title: {String}
  - link: {Object}
  - style: {String}
  - align: {String}
  - new_tab: {Boolean}
{% endcomment %}

{% unless title == blank %}
  <div class="product-form__link__wrapper" style="text-align: {{ align }}">
    <a
      href="{{ link }}"
      class="product-form__link product-form__link--{{ style }}{% unless style == 'body' %} link link--{{ style }}{% endunless %}"
      {% if new_tab %}
        target="_blank"
      {% endif %}
    >
      {{- title | escape -}}
      {%- unless style == 'body' -%}
        {%- render 'icons-theme', icon: 'chevron' -%}
      {%- endunless -%}
    </a>
  </div>
{% endunless %}
